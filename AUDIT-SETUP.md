# Подключение Google Таблицы к аудиту — пошагово

После моих действий сайт уже умеет показывать форму аудита и считать %. Осталось два маленьких шага с вашей стороны (примерно 5 минут), чтобы данные начали сохраняться в вашу Google Таблицу.

---

## Шаг 1. Подключить скрипт к Таблице

1. Откройте вашу Google Таблицу:
   <https://docs.google.com/spreadsheets/d/1VCgqwS9ZlTPGJHHAuPQ8e0CV6dwmF82qIxFw0lYZNVE/edit>
2. В меню сверху: **Расширения → Apps Script**.
3. Откроется редактор кода. Удалите всё, что там есть.
4. Откройте файл `APPS-SCRIPT.gs` в папке `GAISSINA Academy` на рабочем столе, скопируйте всё содержимое и вставьте в Apps Script.
5. Сохраните (значок дискеты или Ctrl+S). Дайте проекту имя — например, **GAISSINA Audit**.

## Шаг 2. Развернуть как веб-приложение

1. В Apps Script нажмите **Развернуть → Новое развёртывание** (Deploy → New deployment).
2. Шестерёнка слева → выберите **«Веб-приложение»** (Web app).
3. Параметры:
   - **Описание:** GAISSINA Audit
   - **Выполнять от имени:** Я (Me) — ваш аккаунт
   - **Кому доступно:** Все (Anyone) — это **обязательно**, иначе сайт не сможет отправлять данные.
4. Нажмите **Развернуть** (Deploy).
5. Google попросит **дать разрешения** — это нормально. Согласитесь:
   - «Дополнительно» → «Перейти к проекту…» → «Разрешить».
6. После развёртывания появится **URL веб-приложения** (Web app URL).
   Он выглядит так: `https://script.google.com/macros/s/AKfyc.../exec`
7. **Скопируйте этот URL и пришлите мне в чат** — я подставлю его в академию, и аудит начнёт сохраняться в Таблицу.

## Шаг 3 (один раз, опционально). Создать красивый дашборд

После того как пришлёт хотя бы один аудит:

1. В Apps Script вверху выберите функцию **`setupDashboard`** в выпадающем списке.
2. Нажмите ▶ **Выполнить**.
3. В Таблице появится лист «Дашборд» с автоматическими сводками:
   - Рейтинг магазинов
   - Разбивка по директорам
   - Последние визиты
   - Топ проваливаемых пунктов

---

## Что появится в Таблице

После первого визита Apps Script сам создаст два листа:

**Лист «Визиты»** — каждая строка = один аудит:
| Время | Дата | Магазин | Аудитор | Директор | % итог | Применимых | ✓ | ✕ | — | Всего | ID |

**Лист «Детали»** — каждая строка = один пункт визита:
| ID визита | Время | Магазин | Аудитор | Директор | Раздел | № | Пункт | Статус | Комментарий |

И отдельно — лист **«Дашборд»** после запуска `setupDashboard()`.

---

## Пароль для входа в аудит

Сейчас стоит временный пароль: **`gaissina-audit-2026`**

Чтобы его поменять — скажите мне, какой пароль установить, и я обновлю. Менять можно в любой момент.

---

## Если что-то пошло не так

- **Пришёл аудит, в Таблице ничего нет** — проверьте, что в шаге 2.3 выбрано «Кому доступно: Все». Без этого Google блокирует запрос с сайта.
- **При нажатии «Развернуть» просит вход** — войдите в свой Google-аккаунт, тот же, что у Таблицы.
- **Хочу другой пароль** — напишите мне, поменяю.
- **Хочу добавить/убрать пункты** — напишите, какие; я обновлю академию.
